# include <iostream> // input-output
# include <cmath> // math
using namespace std ; // namespace

int main(){
    int subscriber ; 
    cin >> subscriber ; 

    if (subscriber < 1000) {
        cout << subscriber << endl ; 
    }

    else { // subscriber >= 1000
        // check if digits is 4,7,10,... (divided by 3 == 1)
        
        // case 1 : digits divided by 3 & remainder 1 -> represents as float (+1 dp) + K

            // TOO HARD , SKIP FIRST


        // case 2 : other case -> return in integer K form
    
    
    }
}